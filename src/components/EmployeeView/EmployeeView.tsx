import React, {JSX} from "react";

export const EmployeeView: React.FC<{}> = (props): JSX.Element => {
    return (
        <div>
            //ToDO Add a disabled form to show the employee data
            // Prop should be employee
            // Prop should be enabled / disabled for editing purposes
        </div>
    )
}
